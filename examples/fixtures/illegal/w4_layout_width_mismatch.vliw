# expected-status: parse-error
.processor {
  width 4

  hardware {
    unit alu = integer_alu
    unit mem = memory
    unit ctrl = control
    unit mul = multiplier
  }

  layout slots {
    0 = { alu }
    1 = { alu }
    2 = { mem }
  }

  cache { }
  topology { cpus 1 }
}

i0 movi r1, 1
x ret
